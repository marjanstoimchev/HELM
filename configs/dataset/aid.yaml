# AID Dataset Configuration
# Aerial Image Dataset - 30 scene classes

name: aid
data_dir: null  # HuggingFace dataset (auto-downloaded)
image_size: 224
num_workers: 16
hierarchy_config_dir: configs/dataset/hierarchies
